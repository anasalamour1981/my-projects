<openerp>
    <data>
        <record id="cm_entity_tree" model="ir.ui.view">
            <field name="name">cm.entity.tree</field>
            <field name="model">cm.entity</field>
            <field name="arch" type="xml">
                <tree string="Entities">
                    <field name="sequence" widget="handle"></field>
                    <field name="code"></field>
                    <field name="name"></field>
                    <field name="type"></field>
                    <field name="parent_id"></field>
                </tree>
            </field>
        </record>

        <record id="cm_entity_form" model="ir.ui.view">
            <field name="name">cm.entity.form</field>
            <field name="model">cm.entity</field>
            <field name="arch" type="xml">
                <form string="Entity">
                    <group>
                        <group>
                            <field name="name" required="True"></field>
                            <field name="code" attrs="{'required': True}"></field>
                            <field name="parent_id" attrs="{'required':[('type', 'in', ['employee'])],'invisible': [('type','in',['external'])]}" domain="[('type','in',['unit'])]"></field>
                            <field name="manager_id" attrs="{'required':[('type', 'in', ['unit'])],'invisible': [('type','not in',['unit'])]}" domain="[('type','in',['employee'])]"></field>
                        </group>
                        <group>
                            <field name="type" required="True"></field>
                            <field name="job_title_id" attrs="{'required':[('type', 'in', ['employee'])],'invisible': [('type','not in',['employee'])]}" widget="selection"></field>
                            <field name="user_id" attrs="{'required':[('type', 'in', ['employee'])],'invisible': [('type','not in',['employee'])]}"></field>
                            <field name="partner_id" invisible="True"></field>
                        </group>
                    </group>
                </form>
            </field>
        </record>
    </data>
</openerp>
